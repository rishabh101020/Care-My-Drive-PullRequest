<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmdrj.caremydriveworkshop">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareMyDriveWorkshop">
        <activity
            android:name=".AddImages"
            android:label="@string/title_activity_add_images"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar"></activity>
        <activity android:name=".ViewImage" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ChooseLocation"
            android:label="@string/title_activity_choose_location" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:exported="true"
            android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ServicesProvided"
            android:label="@string/title_activity_services_provided"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".DetailsBooking"
            android:label="@string/title_activity_details_booking"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".CompServices"
            android:label="@string/title_activity_comp_services"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".LiveServices"
            android:label="@string/title_activity_live_services"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".InitServices"
            android:label="@string/title_activity_init_services"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".EditDetService"
            android:label="@string/title_activity_edit_det_service"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".DetailsService"
            android:label="@string/title_activity_details_service"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CareMyDriveWorkshop.NoActionBar" />
    </application>

</manifest>